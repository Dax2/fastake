var isSquare = function(n){
    squareRoot = Math.sqrt(n);
     if ( n >= 0){
       intCheck = Number.isInteger(squareRoot);
        if (intCheck = true){
        return true
       }
   }    
       else return false;
   } 